#!/bin/bash -e

# If running the rails server then create or migrate the database first.
# (db:prepare is idempotent: loads the schema on an empty DB, migrates otherwise.)
if [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; then
  ./bin/rails db:prepare
fi

exec "${@}"
